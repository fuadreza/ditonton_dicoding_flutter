name: ditonton_dicoding_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+3

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:

  cached_network_image: ^3.2.3
  core:
    path: core
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  firebase_core: ^2.3.0
  firebase_crashlytics: ^3.0.6
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_rating_bar: ^4.0.1
  get_it: ^7.2.0
  google_fonts: ^3.0.1
  http: ^0.13.5
  movie:
    path: movie
  path_provider: ^2.0.11
  sqflite: ^2.2.0+3
  tvseries:
    path: tvseries

dev_dependencies:
  bloc_test: ^9.1.0
  build_runner: ^2.3.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.3.2
  test_cov_console: ^0.2.2

flutter:

  uses-material-design: true

  assets:
    - assets/
    - certificates/
